#include "gtest/gtest.h"


#include "DuplicateFinder.hpp"


using namespace DF;
using DF_t = DuplicateFinder;


TEST(BASE,DF_OPTIONS){
/*     int argc = 6;
    const char* argv[] = {"bayan", "--include", "pupa", "lupa", "--exclude", "pupa"};
    DF_t df(argc, argv);
    EXPECT_STREQ(df.getIncDirs()[0].c_str(), "pupa");
    EXPECT_STREQ(df.getIncDirs()[1].c_str(), "lupa");

    auto excDir = df.getExcDirs();
    auto param = excDir.begin()->c_str();
    EXPECT_STREQ(param, "pupa"); */
}